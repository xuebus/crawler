package com.foofv.crawler.storage

import scala.collection.mutable.ListBuffer
import scala.collection.JavaConversions._

/**
  * @author msfenn
  */

object AccessJava {

  def main(args: Array[String]): Unit = {

    //    var addrList = ListBuffer(new Address("USA"), new Address("China"))
    //    MorphiaTest.save(new User("BILL", 60, addrList));
    //    MorphiaTest.save(new User("JOBS", 50, addrList));
  }
}